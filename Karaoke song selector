#1. Select all the song titles.
#2. SELECT that uses OR to show the titles of the songs that have an 'epic' mood or a release date after 1990.
#3. SELECT that uses AND to show the titles of songs that are 'epic', and released after 1990, and less than 4 minutes long.

#1. 
SELECT title FROM songs;

#2.
SELECT title FROM songs 
WHERE  mood = "epic" OR released >1990;

#3.
SELECT title FROM songs
WHERE mood = "eqic" AND released >1990 AND duration >240;
