#1. Use an UPDATE to emulate what happens when you edit data in the app.
#2. Use a DELETE to emulate what happens when you delete data in the app.


#1. SELECT AVG(minutes) FROM lose_it; SELECT * FROM lose_it WHERE minutes < 35;

#1. SELECT * FROM lose_it; UPDATE lose_it set name = "Zoey" WHERE id = 5;

#2. SELECT * FROM lose_it DELETE FROM lose_it WHERE id = 4;
